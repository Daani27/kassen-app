# WA I Kasse – Umgebungsvariablen (Kopie als .env anlegen, Werte eintragen)
# .env wird von Git ignoriert und darf keine echten Keys ins Repo bringen.

# Supabase (aus dem Supabase-Dashboard: Project Settings → API)
VITE_SUPABASE_URL=https://dein-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=dein-anon-key-hier

# VAPID für Web-Push (nur der öffentliche Key – mit: npx web-push generate-vapid-keys)
# Den gleichen öffentlichen Key auch in Supabase unter Edge Functions → Secrets als VAPID_PUBLIC_KEY eintragen.
VITE_VAPID_PUBLIC_KEY=dein-vapid-public-key-base64url
