# Kassen App – Frontend (Kopie als .env anlegen, Werte eintragen)
# .env wird von Git ignoriert und darf keine echten Keys ins Repo bringen.

# API-URL des Backends (leer = gleiche Domain wie die App, z. B. /api)
VITE_API_URL=

# VAPID öffentlicher Key für Web-Push (npx web-push generate-vapid-keys)
# Derselbe Key wie VAPID_PUBLIC_KEY im Backend (.env im server/)
VITE_VAPID_PUBLIC_KEY=
