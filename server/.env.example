# Datenbank (PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/feuerwehr_kasse

# Nur für Import aus Supabase (scripts/import-from-supabase.js):
# Am einfachsten: Im Dashboard Connect → gewünschten Modus wählen → URI kopieren, hier einfügen.
# Bei Sonderzeichen im Passwort: in der URI [YOUR-PASSWORD] stehen lassen und SUPABASE_DB_PASSWORD setzen.
#
# Oder Varianten (jeweils eigene Zeilen):
# A) SUPABASE_DATABASE_URL=postgresql://postgres:[PASSWORT]@db.xxx.supabase.co:5432/postgres
# B) Direct (5432) oder Transaction (6543) auf db.xxx: SUPABASE_DB_HOST=db.xxx.supabase.co, SUPABASE_DB_PASSWORD=..., ggf. SUPABASE_DB_PORT=6543
# C) Pooler (Session/Transaction): SUPABASE_DB_POOLER_HOST=aws-0-REGION.pooler.supabase.com, SUPABASE_DB_PROJECT_REF=xxx, SUPABASE_DB_PASSWORD=...

# JWT für Login (mind. 32 Zeichen in Produktion)
JWT_SECRET=dein-geheimes-jwt-secret-mind-32-zeichen

# Web-Push (VAPID Keys mit: npx web-push generate-vapid-keys)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
# Für iOS/Safari zwingend: echte mailto:-Adresse mit echter Domain (sonst 403 von Apple)
# z. B. mailto:admin@deine-domain.de (kein noreply@example.com)
VAPID_SUBJECT=mailto:noreply@example.com

# Port des API-Servers
PORT=3001
